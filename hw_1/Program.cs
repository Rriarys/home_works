internal class Program
{
    static void Main()
    {
        //cycles and branching operators
        /*//print prime numbers from 1 to 100

        Console.Write("1, "); //1 делится нацело только на себя же, заносим сразу

        for (int i = 2; i <= 100; i++)  //перебираем кждое
        {
            bool isPrime = true; //флаг, если число простое, он не поменяется (1)->

            for (int j = 2; j <= Math.Sqrt(i); j++) // перебор делителей от 2 до sqrt(число); в первом варианте
                if (i % j == 0)                     // предполагалось делить на весь ряд, но это слишком много вычислений
                    isPrime = false;                // если в этом промежутке найдется делитель, флаг изменится

            if (isPrime) // ->(1) не поменялся, значит для числа не нашлось иных делителей, кроме 1 и самого числа
            {
                Console.Write($"{i}, ");
            }
        }*/


        








    }
}

